# bench — moved

The benchmark dashboard (bench.unsigned.gg) has moved to its own repository with
its own CI/CD:

- **Source:** https://github.com/unsigned-gg/bench
- **Live:** https://bench.unsigned.gg

It was previously `packages/local-models/bench/` here. `MODELS.md` remains the
source of truth for the local-bank rows the dashboard visualizes; the frontier
leaderboard's data now lives in the new repo under `public/data/`.

[project]
name = "agentic-shepherd"
version = "0.0.0"
description = "shepherd runtime-substrate package — pin validated by tests"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3",
    "ruff>=0.8",
]

[tool.ruff]
line-length = 100
